import { PhasePlaceholder } from "@/components/layout/PhasePlaceholder";

export default function IncomePage() {
  return (
    <PhasePlaceholder
      area="Income"
      arriving="v1.1"
      description="Track salary, side income, and one-time inflows. Used to calculate how much you have available for debt payments each month."
    />
  );
}
