import { PhasePlaceholder } from "@/components/layout/PhasePlaceholder";

export default function ExpensesPage() {
  return (
    <PhasePlaceholder
      area="Expenses"
      arriving="v1.1"
      description="Recurring fixed and variable expenses. Subtracted from income to derive your monthly debt-payment budget."
    />
  );
}
